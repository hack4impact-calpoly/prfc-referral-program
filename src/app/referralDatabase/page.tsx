import ReferralDataGrid from "@/components/ReferralDatabase/ReferralDataGrid";

//referral page to display user data
export default function ReferralDatabase() {
  return (
    <div>
      <h1>Referral Database</h1>
      <ReferralDataGrid />
    </div>
  );
}
